{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Run Troca Front",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}"
        },
        {
            "type": "node-terminal",
            "name": "Run Troca Back",
            "request": "launch",
            "command": "npm run test",
            "cwd": "${workspaceFolder}/trocaback"
        }
    ]
}